<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource DialogButton}"/>
        <Setter Property="Foreground" Value="{DynamicResource DialogButtonText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DialogBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
            <Border x:Name="Frame" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="2" Padding="{TemplateBinding Padding}" CornerRadius="4">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" TextBlock.Foreground="{TemplateBinding Foreground}"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="{DynamicResource DialogFocus}"/></Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="{DynamicResource DialogFocus}"/></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Background" Value="{DynamicResource DialogSurface}"/><Setter Property="Foreground" Value="{DynamicResource DialogMuted}"/></Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource DialogForeground}"/>
        <Setter Property="Margin" Value="0,6,0,0"/>
    </Style>
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource DialogSurface}"/>
        <Setter Property="Foreground" Value="{DynamicResource DialogForeground}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource DialogForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DialogBorder}"/>
        <Setter Property="Padding" Value="6"/>
    </Style>
</ResourceDictionary>
