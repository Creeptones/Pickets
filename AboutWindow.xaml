<local:AccessibleDialogWindow x:Class="Pickets.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Pickets"
        Title="About Pickets"
        Width="520" Height="390"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ShowInTaskbar="True"
        Background="{DynamicResource DialogBackground}"
        Foreground="{DynamicResource DialogForeground}"
        FontFamily="Segoe UI">


    <DockPanel Margin="20">
        <Button DockPanel.Dock="Bottom" Content="Close" IsCancel="True" HorizontalAlignment="Right" Margin="0,12,0,0" Click="Close_Click"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock Text="Pickets" FontSize="27" FontWeight="SemiBold"/>
                <TextBlock x:Name="VersionText" Foreground="{DynamicResource DialogMuted}" Margin="0,4,0,16"/>
                <TextBlock FontSize="15" TextWrapping="Wrap" Text="Your desktop, organized without moving your files."/>
                <TextBlock Margin="0,16,0,0" TextWrapping="Wrap" Text="Pickets stores layouts locally, uses no accounts or telemetry, and never uploads your files. Windows may access network locations you reference. Diagnostic copies redact your Windows user profile and username."/>
                <TextBlock Margin="0,12,0,16" Foreground="{DynamicResource DialogMuted}" TextWrapping="Wrap" Text="Independent open-source software under the MIT License. Not affiliated with Stardock."/>
                <WrapPanel>
                    <Button Content="Copy diagnostics" Margin="0,0,8,8" Click="CopyDiagnostics_Click"/>
                    <Button Content="Open data folder" Margin="0,0,8,8" Click="OpenDataFolder_Click"/>
                    <Button Content="View on GitHub" Margin="0,0,8,8" Click="OpenGitHub_Click"/>
                </WrapPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</local:AccessibleDialogWindow>
